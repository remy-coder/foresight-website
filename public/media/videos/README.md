# Videos Directory

Place your large video files (`.mp4`, `.webm`, etc.) in this directory.

To embed a video from this folder in `App.tsx`, simply use the full path relative to the `public/` directory:
```html
<video src="/media/videos/charity-promo.mp4" controls autoPlay loop muted />
```
