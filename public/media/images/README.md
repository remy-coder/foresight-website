# Images Directory

Place your large photos and image files in this directory.

To use an image from this folder in `App.tsx`, simply use the full path relative to the `public/` directory:
```html
<img src="/media/images/your-photo.jpg" alt="Description" />
```
